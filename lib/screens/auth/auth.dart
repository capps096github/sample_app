import '../../sample_app_exporter.dart';

class SampleAppAuth extends StatelessWidget {
  const SampleAppAuth({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
