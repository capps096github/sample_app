import '../../sample_app_exporter.dart';

class SampleAppHome extends StatelessWidget {
  const SampleAppHome({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
