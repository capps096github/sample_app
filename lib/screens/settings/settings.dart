import '../../sample_app_exporter.dart';

class SampleAppSettings extends ConsumerWidget {
  const SampleAppSettings({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return Container();
  }
}
