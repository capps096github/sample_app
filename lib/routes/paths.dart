/// * All paths are defined here for app navigation
library;

/// initial path
const initialPath = homePath;

/// * ------------------------------
/// home path
const homePath = '/';

/// * ------------------------------
/// auth path
const authPath = '/auth';

/// * ------------------------------
/// user path
const userPath = '/user';

/// * ------------------------------
/// settings path
const settingsPath = '/settings';
